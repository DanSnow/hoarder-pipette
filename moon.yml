fileGroups:
  sources:
    - src/**/*
tasks:
  build-logo:
    type: build
    command:
      - tsx
      - build/build-logo.mts
    platform: node
    inputs:
      - assets/logo.svg
    outputs:
      - assets/logo16.png
      - assets/logo48.png
      - assets/logo128.png
      - assets/logo256.png
  build-ext-chrome:
    type: build
    command:
      - wxt
      - zip
    platform: node
    inputs:
      - "@group(sources)"
    outputs:
      - .output/chrome-mv3/**/*
  build-ext-firefox:
    type: build
    command:
      - wxt
      - zip
      - -b
      - firefox
    platform: node
    inputs:
      - "@group(sources)"
    outputs:
      - .output/firefox-mv2/**/*
  build:
    type: build
    command: noop
    deps:
      - build-ext-chrome
      - build-ext-firefox
  dev:
    type: run
    command:
      - wxt
    local: true
    platform: node
  test:
    command:
      - vitest
    preset: watcher
    platform: node
  typecheck:
    type: test
    command: tsc --noEmit
    platform: node
    local: true
  typecheck-watch:
    type: run
    command: tsc --noEmit --watch
    platform: node
    local: true
  generate-client:
    type: build
    command: openapi-ts
    platform: node
